#ifndef DEFS_H
#define DEFS_H

/*
 * common headers
 */
#include <gmp.h>

/*
 * structure prototypes
 */
struct r_expr_t;
struct r_var_t;

struct rvec_expr_t;
struct rvec_var_t;

struct rmat_expr_t;

#endif
